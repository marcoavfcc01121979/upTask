<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body 
        header.barra
            h1 UpTask - Administrador de Projetos 
            a(href="/encerrar-session") Fechar Sessão

        main.contenedor
            aside.contenedor-proyectos
                .panel.crear-proyecto
                    a(href="/nuevo-proyecto" class="boton") Novo Projeto
                        span + 
                .panel.lista-proyectos
                    h2 Projetos
                    ul#proyectos.proyectos
                        if(projetos.length)
                            each projeto in projetos
                                li
                                    a(href=`/projetos/${projeto.url}`)=projeto.nome     
                            
            block contenido

            // pre= vardump(projetos)
            
        script(src="/dist/bundle.js")