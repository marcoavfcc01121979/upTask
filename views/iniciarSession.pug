doctype html
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Iniciar Session em UpTask

            if error
                p.alerta.error= error

            form#formulario.caja.caja-login(method="POST" action="/iniciar-session")
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" placeholder="Seu Email" value="email" ) 
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Seu Password" value="password" )
                .campo.enviar
                    input.boton(type="submit" value="Iniciar Session")
                .campo
                    a(href="/criar-conta") Deseja se cadastrar?

        