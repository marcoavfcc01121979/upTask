doctype html
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1= nomePagina
            p Enviar seu email para restabelecer sua senha

            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error

            form.caja-login(method="POST" action="/restabelecer")
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" placeholder="Seu Email" value="email" ) 
                .campo.enviar
                    input.boton(type="submit" value="Restabelecer Password")
                .campo.acciones
                    a(href="/iniciar-session") Volta a login