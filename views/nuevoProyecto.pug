extends layout

include mixins/criarProjeto

block contenido
    .contenido-principal
        h1 #{nomePagina}

        pre= vardump(projeto)
        
        if(errors)
            each error in errors
                .alerta= error.texto
        +criarProjeto(projeto)

        